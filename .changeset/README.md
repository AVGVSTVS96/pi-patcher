# Changesets

This folder is managed by [`@changesets/cli`](https://github.com/changesets/changesets).

For user-facing changes, run `bun run changeset`, choose the release type, and commit the generated `.changeset/*.md` file. The release workflow turns those changesets into a version PR; merging that PR publishes to npm.
